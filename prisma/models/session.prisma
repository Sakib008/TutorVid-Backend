model Session {
    id String @id @default(uuid())
    name String
    sessionUrl String @unique
    user User @relation(fields: [userId], references: [id])
    userId String
    videos Video[]
    createdAt DateTime @default(now())
}